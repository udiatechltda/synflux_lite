<UserControl x:Class="PDV.Views.ConfiguracoesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="#ECEFF1">

    <UserControl.Resources>

        <!-- Estilo de seção (Recibo, Outros) -->
        <Style x:Key="SectionLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#8A94A6"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,8,0,8"/>
        </Style>

        <!-- Separador -->
        <Style x:Key="SeparatorStyle" TargetType="Separator">
            <Setter Property="Background" Value="#E2E8F0"/>
            <Setter Property="Height" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,0"/>
        </Style>

        <!-- Linha de configuração -->
        <Style x:Key="ConfigRowStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="16,14"/>
            <Setter Property="BorderBrush" Value="#F0F0F0"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>

        <!-- Texto da configuração -->
        <Style x:Key="ConfigLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#2D3748"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Slider customizado -->
        <Style x:Key="SliderStyle" TargetType="Slider">
            <Setter Property="Width" Value="160"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="#1C3E72"/>
            <Setter Property="Minimum" Value="0"/>
            <Setter Property="Maximum" Value="100"/>
        </Style>

        <!-- Toggle Button (Switch) -->
        <Style x:Key="ToggleSwitchStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="48"/>
            <Setter Property="Height" Value="26"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid>
                            <Border x:Name="track"
                                    CornerRadius="13"
                                    Background="#CBD5E0"
                                    Width="48" Height="26"/>
                            <Ellipse x:Name="thumb"
                                     Width="20" Height="20"
                                     Fill="White"
                                     HorizontalAlignment="Left"
                                     Margin="3,0,0,0"
                                     VerticalAlignment="Center">
                                <Ellipse.Effect>
                                    <DropShadowEffect ShadowDepth="1" BlurRadius="3" Opacity="0.3" Color="#000000"/>
                                </Ellipse.Effect>
                            </Ellipse>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="track" Property="Background" Value="#34D399"/>
                                <Setter TargetName="thumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="thumb" Property="Margin" Value="0,0,3,0"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="track" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="ComboBoxConfigStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="6"
                                    BorderBrush="#D1D5DB"
                                    BorderThickness="1"
                                    Padding="10,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left"/>
                                    <TextBlock Grid.Column="1"
                                               Text="∨"
                                               FontSize="10"
                                               Foreground="#6B7280"
                                               VerticalAlignment="Center"
                                               Margin="8,0,0,0"/>
                                </Grid>
                            </Border>
                            <ToggleButton Opacity="0"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                          Focusable="False"/>
                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   Focusable="False">
                                <Border Background="White"
                                        CornerRadius="8"
                                        BorderBrush="#E5E7EB"
                                        BorderThickness="1"
                                        Margin="0,4,0,0">
                                    <ScrollViewer>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#9CA3AF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="Foreground" Value="#111827"/>
                        <Setter Property="Padding" Value="14,8"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="itemBorder"
                                            Background="Transparent"
                                            Padding="{TemplateBinding Padding}">
                                        <DockPanel>
                                            <TextBlock x:Name="checkMark"
                                                       Text="✓"
                                                       FontSize="12"
                                                       Foreground="#111827"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,8,0"
                                                       Visibility="Hidden"
                                                       Width="14"/>
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </DockPanel>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="itemBorder" Property="Background" Value="#F3F4F6"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="itemBorder" Property="Background" Value="#F3F4F6"/>
                                            <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- CARD TOPO -->
            <Border Grid.Row="0"
                    Background="White"
                    CornerRadius="12"
                    Padding="25,20"
                    Margin="0,0,0,15">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Configurações Gerais"
                               FontSize="20"
                               FontWeight="SemiBold"
                               Foreground="#000000"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="Utilize esse espaço para configurar a aplicação"
                               FontSize="12"
                               Foreground="#8A94A6"
                               HorizontalAlignment="Center"
                               Margin="0,6,0,0"/>
                </StackPanel>
            </Border>

            <!-- SEÇÃO: RECIBO -->
            <StackPanel Grid.Row="1" Margin="0,0,0,4">
                <TextBlock Text="Recibo"
                           Style="{StaticResource SectionLabelStyle}"/>
                <Separator Style="{StaticResource SeparatorStyle}"/>
            </StackPanel>

            <!-- CARD RECIBO -->
            <Border Grid.Row="2"
                    Background="White"
                    CornerRadius="12"
                    Margin="0,0,0,15"
                    ClipToBounds="True">
                <StackPanel>

                    <!-- Formato da Página -->
                    <Border Style="{StaticResource ConfigRowStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="🖨"
                                       FontSize="18"
                                       VerticalAlignment="Center"
                                       Margin="0,0,12,0"/>
                            <TextBlock Grid.Column="1"
                                       Text="Formato da Página"
                                       Style="{StaticResource ConfigLabelStyle}"/>
                            <ComboBox Grid.Column="2"
                                      x:Name="cbFormatoPagina"
                                      Style="{StaticResource ComboBoxConfigStyle}"
                                      Width="160"
                                      SelectedIndex="0">
                                <ComboBoxItem Content="Bobina 80"/>
                                <ComboBoxItem Content="Bobina 57"/>
                                <ComboBoxItem Content="A4"/>
                            </ComboBox>
                        </Grid>
                    </Border>

                    <!-- Largura da Página -->
                    <Border Style="{StaticResource ConfigRowStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="📄"
                                       FontSize="16"
                                       VerticalAlignment="Center"
                                       Margin="0,0,12,0"/>
                            <TextBlock Grid.Column="1"
                                       Style="{StaticResource ConfigLabelStyle}">
                                <Run Text="Largura Página ["/>
                                <Run x:Name="runLargura" Text="57.0"/>
                                <Run Text="]"/>
                            </TextBlock>
                            <Slider Grid.Column="2"
                                    x:Name="slLargura"
                                    Style="{StaticResource SliderStyle}"
                                    Minimum="40"
                                    Maximum="210"
                                    Value="57"/>
                        </Grid>
                    </Border>

                    <!-- Margens da Página -->
                    <Border Style="{StaticResource ConfigRowStyle}"
                            BorderThickness="0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="📋"
                                       FontSize="16"
                                       VerticalAlignment="Center"
                                       Margin="0,0,12,0"/>
                            <TextBlock Grid.Column="1"
                                       Style="{StaticResource ConfigLabelStyle}">
                                <Run Text="Margens Página ["/>
                                <Run x:Name="runMargens" Text="5.0"/>
                                <Run Text="]"/>
                            </TextBlock>
                            <Slider Grid.Column="2"
                                    x:Name="slMargens"
                                    Style="{StaticResource SliderStyle}"
                                    Minimum="0"
                                    Maximum="50"
                                    Value="5"/>
                        </Grid>
                    </Border>

                </StackPanel>
            </Border>

            <!-- SEÇÃO: OUTROS -->
            <StackPanel Grid.Row="3" Margin="0,0,0,4">
                <TextBlock Text="Outros"
                           Style="{StaticResource SectionLabelStyle}"/>
                <Separator Style="{StaticResource SeparatorStyle}"/>
            </StackPanel>

            <!-- CARD OUTROS -->
            <Border Grid.Row="4"
                    Background="White"
                    CornerRadius="12"
                    Margin="0,0,0,15"
                    ClipToBounds="True">
                <StackPanel>

                    <!-- Permite Venda com Estoque Negativo -->
                    <Border Style="{StaticResource ConfigRowStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="🔲"
                                       FontSize="16"
                                       VerticalAlignment="Center"
                                       Margin="0,0,12,0"/>
                            <TextBlock Grid.Column="1"
                                       Text="Permite Venda com Estoque Negativo"
                                       Style="{StaticResource ConfigLabelStyle}"/>
                            <ToggleButton Grid.Column="2"
                                          x:Name="tgEstoqueNegativo"
                                          Style="{StaticResource ToggleSwitchStyle}"
                                          IsChecked="True"/>
                        </Grid>
                    </Border>

                    <!-- Operação Fiscal Padrão -->
                    <Border Style="{StaticResource ConfigRowStyle}"
                            BorderThickness="0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0"
                                     x:Name="txtOperacaoFiscal"
                                     Text=""
                                     FontSize="13"
                                     Foreground="#8A94A6"
                                     BorderThickness="0"
                                     Background="Transparent"
                                     VerticalAlignment="Center"
                                     Padding="0">
                                <TextBox.Style>
                                    <Style TargetType="TextBox">
                                        <Style.Triggers>
                                            <Trigger Property="Text" Value="">
                                                <Setter Property="Background">
                                                    <Setter.Value>
                                                        <VisualBrush AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                                            <VisualBrush.Visual>
                                                                <TextBlock Text="Operação Fiscal Padrão"
                                                                           Foreground="#8A94A6"
                                                                           FontSize="13"
                                                                           Padding="2,0"/>
                                                            </VisualBrush.Visual>
                                                        </VisualBrush>
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>
                            <Button Grid.Column="1"
                                    Width="32" Height="32"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Cursor="Hand"
                                    FontSize="16"
                                    Foreground="#8A94A6"
                                    Content="🔍"
                                    VerticalAlignment="Center"/>
                        </Grid>
                    </Border>

                </StackPanel>
            </Border>

        </Grid>
    </ScrollViewer>

</UserControl>