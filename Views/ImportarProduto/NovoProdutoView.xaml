<UserControl x:Class="PDV.Views.ImportarProduto.NovoProdutoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PDV.Views.ImportarProduto"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="#ECEFF1">

    <UserControl.Resources>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F9FAFB"/>
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#EFF6FF"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#DBEAFE"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#1A202C"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Foreground" Value="#1A202C"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="HeaderIconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="34"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="8" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E5E7EB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0" Background="White" CornerRadius="12" Padding="16,14" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Style="{StaticResource HeaderIconButtonStyle}"
                        Command="{Binding FecharCommand}"
                        Margin="0,0,12,0"
                        ToolTip="Voltar">
                    <TextBlock Text="←" FontSize="16" FontWeight="Bold"
                               Foreground="#6B7280" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Button>

                <TextBlock Grid.Column="1" Text="Produtos"
                           FontSize="18" FontWeight="SemiBold" Foreground="#1A202C"
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- Botão Filtro -->

                    <!-- Botão Relatório -->

                    <!-- Botão Novo -->
                    <Button Background="#1C3E72" Foreground="White"
                            Command="{Binding NovoProdutoCommand}"
                            Padding="12,6" BorderThickness="0" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border" Background="{TemplateBinding Background}"
                                        CornerRadius="6" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#2A52A0"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                        <TextBlock Text="+" FontSize="13" FontWeight="SemiBold"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- DATAGRID -->
        <Border Grid.Row="2" Background="White" CornerRadius="12" Padding="10" Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="{Binding InfoResultado}"
                           FontSize="12" Foreground="#6B7280"
                           HorizontalAlignment="Center"
                           Margin="0,5,0,8"/>

                <DataGrid Grid.Row="1"
                          x:Name="DgProdutos"
                          ItemsSource="{Binding Produtos}"
                          SelectedItem="{Binding ProdutoSelecionado}"
                          AutoGenerateColumns="False"
                          Background="White"
                          BorderThickness="0"
                          HeadersVisibility="Column"
                          RowBackground="White"
                          AlternatingRowBackground="#F9FAFB"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          CanUserResizeRows="False"
                          CanUserAddRows="False"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#F3F4F6"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ScrollViewer.CanContentScroll="True">
                    <DataGrid.InputBindings>
                        <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding SelecionarProdutoCommand}"/>
                    </DataGrid.InputBindings>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nome"               Binding="{Binding Nome}"          Width="*"   IsReadOnly="True"/>
                        <DataGridTextColumn Header="Código Interno"     Binding="{Binding CodigoInterno}" Width="140" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Código de Barras (GTIN)" Binding="{Binding Gtin}"    Width="180" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Unidade"            Binding="{Binding Unidade}"       Width="90"  IsReadOnly="True"/>
                        <DataGridTextColumn Header="Tipo"               Binding="{Binding Tipo}"          Width="110" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Descrição"          Binding="{Binding Descricao}"     Width="*"   IsReadOnly="True"/>
                        <DataGridTextColumn Header="SubGrupo"           Binding="{Binding SubGrupo}"      Width="130" IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- PAGINAÇÃO -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,5">
                    <Button Content="⟨⟨" Style="{StaticResource HeaderIconButtonStyle}"
                            Command="{Binding PrimeiraPaginaCommand}" Margin="2,0"/>
                    <Button Content="⟨" Style="{StaticResource HeaderIconButtonStyle}"
                            Command="{Binding PaginaAnteriorCommand}" Margin="2,0"/>
                    <Border Background="#1C3E72" CornerRadius="6" Width="34" Height="34" Margin="2,0">
                        <TextBlock Text="{Binding PaginaAtual, FallbackValue=1}" Foreground="White"
                                   FontSize="13" FontWeight="SemiBold"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <Button Content="⟩" Style="{StaticResource HeaderIconButtonStyle}"
                            Command="{Binding ProximaPaginaCommand}" Margin="2,0"/>
                    <Button Content="⟩⟩" Style="{StaticResource HeaderIconButtonStyle}"
                            Command="{Binding UltimaPaginaCommand}" Margin="2,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- BOTÕES FINAIS -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,0">
            <Button Background="#1C3E72" Foreground="White"
                    Command="{Binding FiltroCommand}"
                    Margin="12" Padding="20,8" BorderThickness="0" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4B5563"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🔍 " FontSize="13"/>
                    <TextBlock Text="Filtro" FontSize="13" FontWeight="SemiBold"/>
                </StackPanel>
            </Button>

            <Button Background="#1C3E72" Foreground="White"
                    Command="{Binding RelatorioCommand}"
                    Margin="12" Padding="20,8" BorderThickness="0" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4B5563"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📄 " FontSize="13"/>
                    <TextBlock Text="Relatório" FontSize="13" FontWeight="SemiBold"/>
                </StackPanel>
            </Button>
        </StackPanel>

    </Grid>
</UserControl>