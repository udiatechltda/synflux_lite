<UserControl x:Class="PDV.Views.EstoqueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="#ECEFF1">

    <UserControl.Resources>

        <!-- Estilo cabeçalho DataGrid -->
        <Style x:Key="ColHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F7FAFC"/>
            <Setter Property="Foreground" Value="#4A5568"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>

        <!-- Estilo célula DataGrid -->
        <Style x:Key="ColCellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#2D3748"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#EBF4FF"/>
                    <Setter Property="Foreground" Value="#1C3E72"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Botão Gera Pedido -->
        <Style x:Key="BtnGeraPedido" TargetType="Button">
            <Setter Property="Background" Value="#1C3E72"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2A52A0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Mesmo estilo do ComboBox do Dashboard -->
        <Style x:Key="ComboBoxDashStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="6"
                                    BorderBrush="#D1D5DB"
                                    BorderThickness="1"
                                    Padding="10,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left"/>
                                    <TextBlock Grid.Column="1"
                                               Text="∨"
                                               FontSize="10"
                                               Foreground="#6B7280"
                                               VerticalAlignment="Center"
                                               Margin="8,0,0,0"/>
                                </Grid>
                            </Border>
                            <ToggleButton Opacity="0"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                          Focusable="False"/>
                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   Focusable="False">
                                <Border Background="White"
                                        CornerRadius="8"
                                        BorderBrush="#E5E7EB"
                                        BorderThickness="1"
                                        Margin="0,4,0,0">
                                    <ScrollViewer>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#9CA3AF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="Foreground" Value="#111827"/>
                        <Setter Property="Padding" Value="14,8"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="itemBorder"
                                            Background="Transparent"
                                            Padding="{TemplateBinding Padding}">
                                        <DockPanel>
                                            <TextBlock x:Name="checkMark"
                                                       Text="✓"
                                                       FontSize="12"
                                                       Foreground="#111827"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,8,0"
                                                       Visibility="Hidden"
                                                       Width="14"/>
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </DockPanel>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="itemBorder" Property="Background" Value="#F3F4F6"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="itemBorder" Property="Background" Value="#F3F4F6"/>
                                            <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- CARD TOPO -->
        <Border Background="White"
                CornerRadius="12"
                Padding="20,14"
                Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Background="Transparent"
                        BorderThickness="0"
                        Command="{Binding VoltarCommand}"
                        Cursor="Hand"
                        Margin="0,0,12,0"
                        Padding="6,4">
                    <TextBlock Text="← Voltar"
                               FontSize="13"
                               Foreground="#1C3E72"
                               FontWeight="SemiBold"/>
                </Button>

                <TextBlock Grid.Column="1"
                           Text="Produtos"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="#000000"
                           VerticalAlignment="Center"/>

                <Button Grid.Column="2"
                        Style="{StaticResource BtnGeraPedido}"
                        Content="⊞  Gera Pedido de Compras"/>
            </Grid>
        </Border>

        <!-- TABELA -->
        <Border Grid.Row="1"
                Background="White"
                CornerRadius="12"
                Margin="0,0,0,10">
            <DataGrid x:Name="dgEstoque"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#F0F0F0"
                      BorderThickness="0"
                      Background="White"
                      RowBackground="White"
                      AlternatingRowBackground="#FAFAFA"
                      ColumnHeaderStyle="{StaticResource ColHeaderStyle}"
                      CellStyle="{StaticResource ColCellStyle}"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Auto">

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <!-- Linha vermelha para estoque crítico (quantidade negativa) -->
                            <DataTrigger Binding="{Binding Critico}" Value="True">
                                <Setter Property="Background" Value="#FFF0F0"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>

                    <DataGridTextColumn Header="Id"
                                        Binding="{Binding Id}"
                                        Width="60"/>

                    <DataGridTextColumn Header="Quantidade Estoque"
                                        Binding="{Binding QuantidadeEstoque, StringFormat=N3}"
                                        Width="150"/>

                    <DataGridTextColumn Header="Estoque Mínimo"
                                        Binding="{Binding EstoqueMinimo, StringFormat=N3}"
                                        Width="130"/>

                    <DataGridTextColumn Header="Estoque Máximo"
                                        Binding="{Binding EstoqueMaximo, StringFormat=N3}"
                                        Width="130"/>

                    <DataGridTextColumn Header="Nome"
                                        Binding="{Binding Nome}"
                                        Width="180"/>

                    <DataGridTextColumn Header="Unidade"
                                        Binding="{Binding Unidade}"
                                        Width="80"/>

                    <DataGridTextColumn Header="Gtin"
                                        Binding="{Binding Gtin}"
                                        Width="140"/>

                    <DataGridTextColumn Header="Código Interno"
                                        Binding="{Binding CodigoInterno}"
                                        Width="120"/>

                    <DataGridTextColumn Header="Descrição"
                                        Binding="{Binding Descricao}"
                                        Width="*"/>

                </DataGrid.Columns>

            </DataGrid>
        </Border>

        <!-- RODAPÉ: filtro + paginação -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- FILTRO STATUS -->
            <Border Background="White"
                    CornerRadius="10"
                    Padding="12,8"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center">
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="Status Estoque"
                               FontSize="11"
                               Foreground="#8A94A6"
                               Margin="0,0,0,4"/>
                    <!-- ComboBox estilizado igual ao Dashboard -->
                    <ComboBox x:Name="cbFiltro"
                              Style="{StaticResource ComboBoxDashStyle}"
                              Width="160"
                              Height="32"
                              SelectedIndex="0">
                        <ComboBoxItem Content="Todos"/>
                        <ComboBoxItem Content="Crítico"/>
                    </ComboBox>
                </StackPanel>
            </Border>

            <!-- PAGINAÇÃO -->
            <Border Grid.Column="1"
                    Background="White"
                    CornerRadius="10"
                    Padding="16,8"
                    VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                    <TextBlock Text="Linhas por página:"
                               FontSize="12"
                               Foreground="#4A5568"
                               VerticalAlignment="Center"
                               Margin="0,0,8,0"/>

                    <!-- ComboBox estilizado igual ao Dashboard -->
                    <ComboBox x:Name="cbLinhas"
                              Style="{StaticResource ComboBoxDashStyle}"
                              Width="65"
                              Height="32"
                              SelectedIndex="0"
                              VerticalContentAlignment="Center">
                        <ComboBoxItem Content="10"/>
                        <ComboBoxItem Content="25"/>
                        <ComboBoxItem Content="50"/>
                    </ComboBox>

                    <TextBlock x:Name="txtPaginacao"
                               Text="1 a 10 de 0"
                               FontSize="12"
                               Foreground="#4A5568"
                               VerticalAlignment="Center"
                               Margin="16,0"/>

                    <Button Content="❮"
                            Width="28" Height="28"
                            Background="Transparent"
                            BorderThickness="0"
                            FontSize="14"
                            Foreground="#4A5568"
                            Cursor="Hand"/>

                    <Button Content="❯"
                            Width="28" Height="28"
                            Background="Transparent"
                            BorderThickness="0"
                            FontSize="14"
                            Foreground="#4A5568"
                            Cursor="Hand"/>

                </StackPanel>
            </Border>

        </Grid>

    </Grid>
</UserControl>
