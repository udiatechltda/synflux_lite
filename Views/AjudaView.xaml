<UserControl x:Class="PDV.Views.AjudaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#ECEFF1">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header com botão voltar -->
        <Border Grid.Row="0" Background="White" CornerRadius="12" Padding="20,14" Margin="20,20,20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Background="Transparent" BorderThickness="0"
                        Command="{Binding VoltarCommand}" Cursor="Hand" Margin="0,0,12,0" Padding="6,4">
                    <TextBlock Text="← Voltar" FontSize="13" Foreground="#1C3E72" FontWeight="SemiBold"/>
                </Button>
                <TextBlock Grid.Column="1" Text="Ajuda - Tutoriais em Vídeo"
                           FontSize="18" FontWeight="SemiBold" Foreground="#1A202C" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Conteúdo principal -->
        <Border Grid.Row="1" Background="White" CornerRadius="12" Margin="20,0,20,20">
            <Grid Margin="40">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Ícone de ajuda -->
                <Border Grid.Row="0" Width="80" Height="80" CornerRadius="40" Background="#1C3E72" HorizontalAlignment="Center" Margin="0,20,0,20">
                    <TextBlock Text="?" FontSize="48" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <!-- Título -->
                <TextBlock Grid.Row="1" Text="Precisa de ajuda com o sistema?" 
                           FontSize="20" FontWeight="SemiBold" Foreground="#1A202C" 
                           HorizontalAlignment="Center" Margin="0,0,0,10"/>

                <!-- Descrição -->
                <TextBlock Grid.Row="2" Text="Clique no botão abaixo para acessar nossa playlist de tutoriais no YouTube"
                           FontSize="14" Foreground="#4A5568" HorizontalAlignment="Center" 
                           TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,30"/>

                <!-- Botão para acessar o YouTube -->
                <Button Grid.Row="3" 
                        Width="300" Height="50"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Top"
                        Command="{Binding AbrirYouTubeCommand}"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#FF0000"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" 
                                                Background="{TemplateBinding Background}" 
                                                CornerRadius="25" 
                                                Padding="20,0">
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <TextBlock Text="▶ " FontSize="16" Margin="0,0,5,0"/>
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#CC0000"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#990000"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                    <TextBlock Text="Assistir Tutoriais no YouTube"/>
                </Button>

                <!-- Texto informativo do link -->
                <TextBlock Grid.Row="4" FontSize="11" Foreground="#8A94A6" 
                           HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,20,0,20"
                           Text="Link da playlist: https://www.youtube.com/playlist?list=PLMqoOoxxICPfegpWl8Mj2mthn5QocLGuL"
                           TextWrapping="Wrap" TextAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>