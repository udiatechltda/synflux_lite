<UserControl x:Class="PDV.Views.SairView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#ECEFF1">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header com botão voltar -->
        <Border Grid.Row="0" Background="White" CornerRadius="12" Padding="20,14" Margin="20,20,20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Background="Transparent" BorderThickness="0"
                        Command="{Binding VoltarCommand}" Cursor="Hand" Margin="0,0,12,0" Padding="6,4">
                    <TextBlock Text="← Voltar" FontSize="13" Foreground="#1C3E72" FontWeight="SemiBold"/>
                </Button>
                <TextBlock Grid.Column="1" Text="Sair do Sistema"
                           FontSize="18" FontWeight="SemiBold" Foreground="#1A202C" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Conteúdo principal -->
        <Border Grid.Row="1" Background="White" CornerRadius="12" Margin="20,0,20,20">
            <Grid Margin="40">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Ícone de saída -->
                <Border Grid.Row="0" Width="80" Height="80" CornerRadius="40" Background="#1C3E72" HorizontalAlignment="Center" Margin="0,20,0,20">
                    <TextBlock Text="🚪" FontSize="40" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <!-- Título -->
                <TextBlock Grid.Row="1" Text="Deseja sair da aplicação?" 
                           FontSize="22" FontWeight="SemiBold" Foreground="#1A202C" 
                           HorizontalAlignment="Center" Margin="0,0,0,15"/>

                <!-- Descrição -->
                <TextBlock Grid.Row="2" Text="Tem certeza que deseja encerrar o sistema?"
                           FontSize="14" Foreground="#4A5568" HorizontalAlignment="Center" 
                           TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,40"/>

                <!-- Botões de ação -->
                <Grid Grid.Row="3" HorizontalAlignment="Center" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Botão Cancelar -->
                    <Button Grid.Column="0" 
                                Width="150" Height="45"
                                Command="{Binding CancelarCommand}"
                                Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="Foreground" Value="#1C3E72"/>
                                <Setter Property="FontSize" Value="14"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="BorderBrush" Value="#D0D5DD"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border" 
                                                        Background="{TemplateBinding Background}" 
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="8" 
                                                        Padding="20,0">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#F7FAFC"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="✖️ " FontSize="14" Margin="0,0,8,0"/>
                            <TextBlock Text="Cancelar"/>
                        </StackPanel>
                    </Button>

                    <!-- Botão Confirmar Sair -->
                    <Button Grid.Column="2" 
                                Width="150" Height="45"
                                Command="{Binding ConfirmarSairCommand}"
                                Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#1C3E72"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontSize" Value="14"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border" 
                                                        Background="{TemplateBinding Background}" 
                                                        CornerRadius="8" 
                                                        Padding="20,0">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#C53030"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#9B2C2C"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="✓ " FontSize="14" Margin="0,0,8,0"/>
                            <TextBlock Text="Sair"/>
                        </StackPanel>
                    </Button>

                </Grid>

                <!-- Mensagem de aviso -->
                <Border Grid.Row="4" Background="#FFF3E0" CornerRadius="8" Padding="15" Margin="0,20,0,0">
                    <TextBlock Text="⚠️ Atenção: Todas as alterações não salvas serão perdidas!"
                               FontSize="12" Foreground="#E65100" 
                               HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"/>
                </Border>

            </Grid>
        </Border>
    </Grid>
</UserControl>