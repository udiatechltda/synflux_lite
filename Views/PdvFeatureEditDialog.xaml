<Window x:Class="PDV.Views.PdvFeatureEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Titulo}"
        Width="680"
        Height="720"
        WindowStartupLocation="CenterOwner"
        Background="#ECEFF1">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="White" CornerRadius="8" Padding="18" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="{Binding Titulo}" FontSize="20" FontWeight="SemiBold" Foreground="#1F2937"/>
                <TextBlock Text="Edite os campos do cadastro selecionado." FontSize="12" Foreground="#64748B" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="18">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Campos}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Width="292" Margin="0,0,16,14">
                                <TextBlock Text="{Binding Label}" FontSize="12" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,5"/>
                                <TextBox Text="{Binding ValorTexto, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         MinHeight="38"
                                         Padding="10,8"
                                         FontSize="13"
                                         BorderBrush="#D8DEE9"
                                         BorderThickness="1"
                                         Background="White"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="14" Margin="0,12,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancelar"
                        Width="120"
                        Height="38"
                        Margin="0,0,10,0"
                        Click="Cancelar_Click"/>
                <Button Content="Salvar"
                        Width="140"
                        Height="38"
                        Background="#1C3E72"
                        Foreground="White"
                        BorderThickness="0"
                        FontWeight="SemiBold"
                        Click="Salvar_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
