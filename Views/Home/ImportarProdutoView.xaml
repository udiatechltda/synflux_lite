<UserControl x:Class="PDV.Views.ImportarProdutoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="#ECEFF1">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>

        <Style x:Key="ComboBoxFieldStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="border" Background="{TemplateBinding Background}"
                                    CornerRadius="6" BorderBrush="#D1D5DB" BorderThickness="1" Padding="10,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="∨" FontSize="10"
                                               Foreground="#6B7280" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                </Grid>
                            </Border>
                            <ToggleButton Opacity="0"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                          Focusable="False"/>
                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom" AllowsTransparency="True" Focusable="False">
                                <Border Background="White" CornerRadius="8"
                                        BorderBrush="#E5E7EB" BorderThickness="1" Margin="0,4,0,0">
                                    <ScrollViewer MaxHeight="200">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#9CA3AF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="Foreground" Value="#111827"/>
                        <Setter Property="Padding" Value="14,8"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="itemBorder" Background="Transparent" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="itemBorder" Property="Background" Value="#F3F4F6"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="itemBorder" Property="Background" Value="#EFF6FF"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F9FAFB"/>
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#EFF6FF"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#DBEAFE"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#1A202C"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Foreground" Value="#1A202C"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="HeaderIconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="34"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="8" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E5E7EB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ScrollArrowButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#EFF6FF"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#BFDBFE"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#DBEAFE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0" Background="White" CornerRadius="12" Padding="16,14" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Style="{StaticResource HeaderIconButtonStyle}"
                        Command="{Binding FecharCommand}"
                        Margin="0,0,12,0"
                        ToolTip="Fechar">
                    <TextBlock Text="✕" FontSize="14" FontWeight="Bold"
                               Foreground="#6B7280" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Button>

                <TextBlock Grid.Column="1" Text="Importar Produto"
                           FontSize="18" FontWeight="SemiBold" Foreground="#1A202C"
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource HeaderIconButtonStyle}"
                            Command="{Binding PesquisarCommand}"
                            Margin="0,0,8,0"
                            ToolTip="Pesquisar">
                        <Path Width="14" Height="14"
                              Stroke="#6B7280" StrokeThickness="1.8"
                              Data="M11 11 L16 16 M13 7 A6 6 0 1 1 1 7 A6 6 0 1 1 13 7"
                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>

                    <Button Background="#1C3E72" Foreground="White"
                            Command="{Binding NovoProdutoCommand}"
                            Padding="12,6" BorderThickness="0" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border" Background="{TemplateBinding Background}"
                                        CornerRadius="6" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#2A52A0"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                        <TextBlock Text="+" FontSize="13" FontWeight="SemiBold"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- FILTROS -->
        <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="25" Margin="0,0,0,15">
            <StackPanel>

                <!-- Campo de busca -->
                <Grid Margin="0,0,0,15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Campo de busca" FontSize="12" Foreground="#8A94A6" Margin="0,0,0,5"/>

                    <ComboBox Grid.Row="1" Style="{StaticResource ComboBoxFieldStyle}"
                              SelectedItem="{Binding CampoBuscaSelecionado}"
                              SelectedIndex="0">
                        <ComboBoxItem Content="Id"/>
                        <ComboBoxItem Content="Gtin"/>
                        <ComboBoxItem Content="Codigo Interno"/>
                        <ComboBoxItem Content="Nome"/>
                        <ComboBoxItem Content="Descricao"/>
                        <ComboBoxItem Content="Valor Compra"/>
                        <ComboBoxItem Content="Valor Venda"/>
                        <ComboBoxItem Content="Quantidade Estoque"/>
                        <ComboBoxItem Content="Estoque Minimo"/>
                        <ComboBoxItem Content="Estoque Maximo"/>
                        <ComboBoxItem Content="Codigo Ncm"/>
                        <ComboBoxItem Content="Codigo Cest"/>
                        <ComboBoxItem Content="Ippt"/>
                        <ComboBoxItem Content="Situacao"/>
                    </ComboBox>
                </Grid>

                <!-- Valor e Buscar -->
                <Grid Margin="0,0,0,15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3"
                               Text="Valor" FontSize="12" Foreground="#8A94A6" Margin="0,0,0,0"/>

                    <Border Grid.Row="1" Grid.Column="0" 
                            CornerRadius="6" BorderBrush="#D1D5DB" BorderThickness="1" Background="White" Height="35">
                        <TextBox x:Name="TxtPesquisa" FontSize="13" Background="Transparent"
                                 BorderThickness="0" VerticalContentAlignment="Center"
                                 Foreground="#111827" Padding="10,0"
                                 Text="{Binding TermoPesquisa, UpdateSourceTrigger=PropertyChanged}">
                            <TextBox.InputBindings>
                                <KeyBinding Key="Return" Command="{Binding PesquisarCommand}"/>
                            </TextBox.InputBindings>
                        </TextBox>
                    </Border>

                    <Button Grid.Row="1" Grid.Column="2" Background="#1C3E72" Foreground="White"
                            Command="{Binding PesquisarCommand}"
                            Padding="20,0" Height="35" BorderThickness="0" Cursor="Hand"
                            VerticalAlignment="Stretch">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border" Background="{TemplateBinding Background}"
                                        CornerRadius="6" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#2A52A0"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                        <TextBlock Text="Buscar" FontSize="13" FontWeight="SemiBold"/>
                    </Button>
                </Grid>

                <TextBlock Text="Digite 3 ou mais caracteres ou tecle Enter para pesquisar."
                           FontSize="11" Foreground="#A0AEC0" Margin="0,0,0,0"
                           Visibility="{Binding MostrarDica, Converter={StaticResource BoolToVisibility}}"/>
            </StackPanel>
        </Border>

        <!-- DATAGRID -->
        <Border Grid.Row="2" Background="White" CornerRadius="12" Padding="10" Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="{Binding InfoResultado}"
                           FontSize="12" Foreground="#6B7280"
                           HorizontalAlignment="Center"
                           Margin="0,5,0,8"/>

                <ScrollViewer Grid.Row="1"
                              x:Name="svDataGrid"
                              HorizontalScrollBarVisibility="Hidden"
                              VerticalScrollBarVisibility="Auto">
                    <DataGrid x:Name="DgProdutos"
                              ItemsSource="{Binding Produtos}"
                              SelectedItem="{Binding ProdutoSelecionadoItem}"
                              AutoGenerateColumns="False"
                              Background="White"
                              BorderThickness="0"
                              HeadersVisibility="Column"
                              RowBackground="White"
                              AlternatingRowBackground="#F9FAFB"
                              SelectionMode="Single"
                              SelectionUnit="FullRow"
                              CanUserResizeRows="False"
                              CanUserAddRows="False"
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="#F3F4F6"
                              ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                              ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <DataGrid.InputBindings>
                            <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding SelecionarProdutoCommand}"/>
                        </DataGrid.InputBindings>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Id"             Binding="{Binding Id}"                                        Width="55"  IsReadOnly="True"/>
                            <DataGridTextColumn Header="Gtin"           Binding="{Binding Gtin}"                                      Width="140" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Código Interno" Binding="{Binding CodigoInterno}"                             Width="120" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Nome"           Binding="{Binding Nome}"                                      Width="180" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Descrição"      Binding="{Binding Descricao}"                                 Width="180" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Valor Compra"   Binding="{Binding ValorCompra,   StringFormat={}{0:N2}}"      Width="105" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Valor Venda"    Binding="{Binding ValorVenda,    StringFormat={}{0:N2}}"      Width="105" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Qtd Estoque"    Binding="{Binding QuantidadeEstoque, StringFormat={}{0:N2}}"  Width="110" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Estoque Mínimo" Binding="{Binding EstoqueMinimo, StringFormat={}{0:N2}}"      Width="120" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Estoque Máximo" Binding="{Binding EstoqueMaximo, StringFormat={}{0:N2}}"      Width="120" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Código NCM"     Binding="{Binding CodigoNcm}"                                 Width="110" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Código CEST"    Binding="{Binding CodigoCest}"                                Width="110" IsReadOnly="True"/>
                            <DataGridTextColumn Header="IPPT"           Binding="{Binding Ippt}"                                      Width="70"  IsReadOnly="True"/>
                            <DataGridTextColumn Header="Situação"       Binding="{Binding Situacao}"                                  Width="80"  IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </ScrollViewer>

                <!-- SETAS DE SCROLL HORIZONTAL -->
                <Border Grid.Row="2" Background="White" Padding="0,6,0,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Style="{StaticResource ScrollArrowButtonStyle}"
                                Margin="0,0,6,0"
                                Click="BtnScrollEsquerda_Click">
                            <TextBlock Text="‹" FontSize="16" Foreground="#4A5568"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                       Margin="0,-1,0,0"/>
                        </Button>
                        <Button Style="{StaticResource ScrollArrowButtonStyle}"
                                Click="BtnScrollDireita_Click">
                            <TextBlock Text="›" FontSize="16" Foreground="#4A5568"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                       Margin="0,-1,0,0"/>
                        </Button>
                    </StackPanel>
                </Border>

            </Grid>
        </Border>

    </Grid>
</UserControl>