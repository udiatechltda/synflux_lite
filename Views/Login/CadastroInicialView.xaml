<UserControl x:Class="PDV.Views.CadastroInicialView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:behaviors="clr-namespace:PDV.Utilities.Behaviors"
             Background="{StaticResource BackgroundDefaulBrush}"
             mc:Ignorable="d"
             d:DesignWidth="1024"
             d:DesignHeight="600">

    <UserControl.Resources>
        <Style x:Key="CadastroInputBorderStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D0D5DD"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Height" Value="48"/>
        </Style>

        <Style x:Key="CadastroTextBoxStyle" TargetType="TextBox">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="CadastroLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#8A94A6"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="CadastroComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="itemBorder" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="itemBorder" Property="Background" Value="#EEF3FA"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="itemBorder" Property="Background" Value="#E6EEF8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CadastroComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="14,0,38,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource CadastroComboBoxItemStyle}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          Focusable="False"
                                          ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="toggleBorder" Background="Transparent" CornerRadius="8">
                                            <Grid>
                                                <Path Data="M 0 0 L 4 4 L 8 0"
                                                      Stroke="#8A94A6"
                                                      StrokeThickness="1.6"
                                                      StrokeStartLineCap="Round"
                                                      StrokeEndLineCap="Round"
                                                      HorizontalAlignment="Right"
                                                      VerticalAlignment="Center"
                                                      Margin="0,0,14,0"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="toggleBorder" Property="Background" Value="#F8FAFD"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <ContentPresenter x:Name="ContentSite"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>

                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   PopupAnimation="Slide">
                                <Border Background="White"
                                        BorderBrush="#D0D5DD"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        Padding="0,4">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black" BlurRadius="12" ShadowDepth="2" Opacity="0.10"/>
                                    </Border.Effect>
                                    <ScrollViewer MaxHeight="180" CanContentScroll="True">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#9CA3AF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StepDashStyle" TargetType="Border">
            <Setter Property="Width" Value="58"/>
            <Setter Property="Height" Value="5"/>
            <Setter Property="CornerRadius" Value="3"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="SecondaryWizardButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="48"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="White" BorderBrush="#1C3E72" BorderThickness="1" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#EEF3FA"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="BorderBrush" Value="#D0D5DD"/>
                                <Setter TargetName="border" Property="Background" Value="#F8FAFD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryWizardButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="48"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="#1C3E72" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2A52A0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#9CA3AF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource BackgroundDefaulBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#1DB8FF" Offset="0"/>
                    <GradientStop Color="#0089E0" Offset="0.6"/>
                    <GradientStop Color="#0055A4" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>

            <Image Source="/Assets/Images/Png/bg_login.png"
                   Stretch="Uniform"
                   VerticalAlignment="Bottom"
                   HorizontalAlignment="Center"
                   Width="554"
                   Height="588"
                   Margin="0,45,0,0"/>

            <StackPanel Margin="20" VerticalAlignment="Top">
                <TextBlock Text="Sistema PDV" FontSize="35" FontWeight="Bold" Foreground="White" Margin="0,20,0,0"/>
                <TextBlock Text="Para gerenciar seu negócio" FontSize="16" Foreground="White"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="24*"/>
                <ColumnDefinition Width="520"/>
                <ColumnDefinition Width="25*"/>
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <Border Background="White"
                        CornerRadius="12"
                        Padding="36"
                        Width="520"
                        MinHeight="560"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="16" ShadowDepth="2" Opacity="0.08"/>
                    </Border.Effect>

                    <StackPanel>
                        <TextBlock Text="Cadastro Inicial" FontSize="28" FontWeight="SemiBold" Foreground="#1A202C" Margin="0,0,0,6" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding StepTitle}" FontSize="13" Foreground="#999999" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="{Binding ProgressText}" FontSize="13" FontWeight="SemiBold" Foreground="#1C3E72" HorizontalAlignment="Center" Margin="0,0,0,12"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,22">
                            <Border Style="{StaticResource StepDashStyle}" Background="{Binding StepEmpresaBrush}" Margin="0,0,8,0"/>
                            <Border Style="{StaticResource StepDashStyle}" Background="{Binding StepUsuarioBrush}" Margin="0,0,8,0"/>
                            <Border Style="{StaticResource StepDashStyle}" Background="{Binding StepSegurancaBrush}"/>
                        </StackPanel>

                        <TextBlock Text="{Binding StepSubtitle}" FontSize="13" Foreground="#999999" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,22"/>

                        <Grid MinHeight="240">
                            <StackPanel Visibility="{Binding IsEmpresaStep, Converter={StaticResource BoolToVisibility}}">
                                <TextBlock Text="CNPJ" Style="{StaticResource CadastroLabelStyle}"/>
                                <Border Style="{StaticResource CadastroInputBorderStyle}" Margin="0,0,0,18">
                                    <TextBox Text="{Binding Cnpj, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource CadastroTextBoxStyle}"/>
                                </Border>

                                <TextBlock Text="Razão social" Style="{StaticResource CadastroLabelStyle}"/>
                                <Border Style="{StaticResource CadastroInputBorderStyle}" Margin="0,0,0,18">
                                    <TextBox Text="{Binding RazaoSocial, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource CadastroTextBoxStyle}"/>
                                </Border>

                                <TextBlock Text="Nome fantasia" Style="{StaticResource CadastroLabelStyle}"/>
                                <Border Style="{StaticResource CadastroInputBorderStyle}">
                                    <TextBox Text="{Binding NomeFantasia, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource CadastroTextBoxStyle}"/>
                                </Border>
                            </StackPanel>

                            <StackPanel Visibility="{Binding IsUsuarioStep, Converter={StaticResource BoolToVisibility}}">
                                <TextBlock Text="E-mail de confirmação" Style="{StaticResource CadastroLabelStyle}"/>
                                <Border Style="{StaticResource CadastroInputBorderStyle}" Margin="0,0,0,18">
                                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource CadastroTextBoxStyle}"/>
                                </Border>

                                <TextBlock Text="Nome do usuário" Style="{StaticResource CadastroLabelStyle}"/>
                                <Border Style="{StaticResource CadastroInputBorderStyle}" Margin="0,0,0,18">
                                    <TextBox Text="{Binding UsuarioNome, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource CadastroTextBoxStyle}"/>
                                </Border>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Login" Style="{StaticResource CadastroLabelStyle}"/>
                                        <Border Style="{StaticResource CadastroInputBorderStyle}">
                                            <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource CadastroTextBoxStyle}"/>
                                        </Border>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Perfil" Style="{StaticResource CadastroLabelStyle}"/>
                                        <Border Style="{StaticResource CadastroInputBorderStyle}">
                                            <ComboBox SelectedValue="{Binding Perfil, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Content" Style="{StaticResource CadastroComboBoxStyle}">
                                                <ComboBoxItem Content="Administrador"/>
                                                <ComboBoxItem Content="Gerente"/>
                                                <ComboBoxItem Content="Operador"/>
                                            </ComboBox>
                                        </Border>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>

                            <StackPanel Visibility="{Binding IsSegurancaStep, Converter={StaticResource BoolToVisibility}}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Senha" Style="{StaticResource CadastroLabelStyle}"/>
                                        <Border Style="{StaticResource CadastroInputBorderStyle}">
                                            <PasswordBox x:Name="SenhaInput" BorderThickness="0" Padding="14,0" FontSize="15" VerticalContentAlignment="Center" PasswordChanged="SenhaInput_PasswordChanged"/>
                                        </Border>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Confirmar senha" Style="{StaticResource CadastroLabelStyle}"/>
                                        <Border Style="{StaticResource CadastroInputBorderStyle}">
                                            <PasswordBox x:Name="ConfirmarSenhaInput" BorderThickness="0" Padding="14,0" FontSize="15" VerticalContentAlignment="Center" PasswordChanged="ConfirmarSenhaInput_PasswordChanged"/>
                                        </Border>
                                    </StackPanel>
                                </Grid>

                                <Border Background="#F8FAFD" CornerRadius="8" BorderBrush="#E5EAF1" BorderThickness="1" Padding="12" Margin="0,18,0,0">
                                    <TextBlock Text="Após concluir, você seguirá para a confirmação do registro antes de acessar o PDV." FontSize="13" Foreground="#64748B" TextWrapping="Wrap"/>
                                </Border>
                            </StackPanel>
                        </Grid>

                        <Border Background="#EEF1F5" CornerRadius="6" Margin="0,18,0,0" Padding="10" Visibility="{Binding HasMensagem, Converter={StaticResource BoolToVisibility}}">
                            <TextBlock Text="{Binding Mensagem}" FontSize="12" Foreground="#1A202C" TextWrapping="Wrap"/>
                        </Border>

                        <Grid Margin="0,24,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Button Grid.Column="0" Width="110" Style="{StaticResource SecondaryWizardButtonStyle}" Command="{Binding CancelarCommand}">
                                <TextBlock Text="Cancelar" FontSize="14" FontWeight="SemiBold" Foreground="#64748B"/>
                            </Button>

                            <Button Grid.Column="2" Width="110" Style="{StaticResource SecondaryWizardButtonStyle}" Command="{Binding VoltarCommand}" IsEnabled="{Binding CanGoBack}">
                                <TextBlock Text="Voltar" FontSize="14" FontWeight="SemiBold" Foreground="#1C3E72"/>
                            </Button>

                            <Button Grid.Column="4" Width="130" Style="{StaticResource PrimaryWizardButtonStyle}" Command="{Binding AvancarCommand}" Visibility="{Binding IsNotLastStep, Converter={StaticResource BoolToVisibility}}">
                                <TextBlock Text="Avançar" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                            </Button>

                            <Button Grid.Column="4" Width="130" Style="{StaticResource PrimaryWizardButtonStyle}" Command="{Binding CadastrarCommand}" behaviors:ButtonLoading.IsEnabled="True" IsEnabled="{Binding IsNotLoading}" Visibility="{Binding IsLastStep, Converter={StaticResource BoolToVisibility}}">
                                <TextBlock Text="Concluir" FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                            </Button>
                        </Grid>
                    </StackPanel>
                </Border>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
