<Window x:Class="PDV.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:PDV.Views"
        mc:Ignorable="d"
        Title="Sistema PDV" 
        WindowStartupLocation="CenterScreen"
        MinWidth="1024"
        MinHeight="600">

    <Window.Resources>
        <!-- Definir os recursos necessários localmente -->
        <SolidColorBrush x:Key="BackgroundDefaulBrush" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="PlaceholderBrush" Color="#666666"/>
        <SolidColorBrush x:Key="InputBorderBrush" Color="#DDDDDD"/>
        <SolidColorBrush x:Key="TitleBrush" Color="#333333"/>
        <SolidColorBrush x:Key="PrimarioBrush" Color="#2E7DE1"/>

        <!-- Estilo do Botão Primário -->
        <Style x:Key="PrimarioButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimarioBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1A5FB4"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                    <Setter Property="Foreground" Value="#666666"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="AlertContainerStyle" TargetType="ItemsControl">
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <StackPanel Margin="10" VerticalAlignment="Top" HorizontalAlignment="Right"/>
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AlertItemStyle" TargetType="Border">
            <Setter Property="Background" Value="{Binding BackgroundColor}"/>
            <Setter Property="BorderBrush" Value="{Binding BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="MaxWidth" Value="400"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Área de Alerts no canto superior direito -->
        <ItemsControl x:Name="AlertsContainer"
                      ItemsSource="{Binding AlertService.Alerts}"
                      Style="{StaticResource AlertContainerStyle}"
                      Panel.ZIndex="1000">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource AlertItemStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" 
                                       Text="{Binding Message}" 
                                       Foreground="{Binding TextColor}"
                                       TextWrapping="Wrap"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center"/>

                            <Button Grid.Column="1"
                                    Content="×"
                                    Foreground="{Binding TextColor}"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    FontSize="16"
                                    FontWeight="Bold"
                                    Padding="8,0"
                                    Cursor="Hand"
                                    Click="DismissAlert_Click">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="Transparent">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#20000000"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Conteúdo principal -->
        <ContentControl x:Name="MainContent"/>
    </Grid>
</Window>